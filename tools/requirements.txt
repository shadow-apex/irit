# Dependencies cho các script trong thư mục /tools
# Cài đặt bằng: pip install -r tools/requirements.txt
#
# Lưu ý: các script này chỉ chạy được trên Windows (dùng ctypes.windll,
# WMI, PowerShell...).

pyautogui       # ai_vision.py (chụp màn hình), mouse_control.py, ocr_region.py, color_picker.py, api_server.py dùng chung
pyperclip       # clipboard_manager.py, clipboard_history.py, api_server.py (/type endpoint)
psutil          # sys_monitor.py (CPU/RAM/Disk/Battery), active_window_info.py, process_manager.py
pygetwindow     # magic_move.py (tìm & di chuyển cửa sổ)
plyer           # notifier.py, quick_reminder.py (thông báo hệ thống)
pytesseract     # ocr_region.py — CẦN CÀI THÊM Tesseract-OCR engine riêng (không phải gói pip):
                #   https://github.com/UB-Mannheim/tesseract/wiki
pyttsx3         # tts_speak.py (đọc văn bản offline qua SAPI5)

# Không cần cài thêm gì (chỉ dùng ctypes/subprocess có sẵn trong Python):
#   idle_time.py, lock_screen.py, multi_monitor_info.py, active_window_info.py
#   power_plan.py, wifi_manager.py, focus_assist.py (powercfg/netsh/user32.dll)
